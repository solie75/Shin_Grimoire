```diff
+ 자, 다음 단계는 서버 SDK와 언리얼 엔진용 플러그인을 다운로드하는 것입니다.
- So the next step is to download the server SDK and the plugin for unreal.

+ 이 링크를 따라가시면 됩니다. 물론 리소스(강의 자료)에도 링크가 포함되어 있습니다.
- So we can follow this link. It'll also be in the resources of course.

+ 여기서 스크롤을 내려 'Unreal Engine'이 선택되어 있는지 확인하시고, 조금 더 아래로 내려보면 'Get started for Unreal Engine' 항목이 보일 겁니다.
- And from here we can scroll down making sure Unreal Engine is selected and a little bit lower. We have git started for Unreal Engine.

+ 이걸 클릭하면 또 다른 GitHub 저장소로 이동하는데, 스크롤을 내리면 GitHub 릴리스 페이지에서 언리얼용 GameLift 플러그인을 zip 파일로 다운로드할 수 있습니다.
- This takes us to yet another GitHub repo, and if we scroll down, we'll see that we can download the Gamelift plugin for unreal as a zip file from the GitHub releases page.

+ 클릭해서 이동한 페이지에서 압축을 풀기 쉬운 zip 파일을 다운로드할 수 있습니다. 자, 다운로드해 보죠.
- If we click on that, we're taken to this page where we can download an easy to extract zip file. So let's go ahead and get that.

+ 다운로드 폴더를 열어서 압축을 풀겠습니다. 우클릭 후 '모두 압축 풀기(Extract All)'를 해서 바로 여기에 풀겠습니다.
- We'll open it in our downloads folder here and we'll extract it. I'm just going to right click and extract all and just extract it here.

+ 압축을 풀면 그 안에 또 두 개의 zip 파일이 들어있을 겁니다. 하나는 'Amazon GameLift Plugin for Unreal'이고, 다른 하나는 'GameLift C++ Server SDK'입니다.
- And we're going to have two zipped up files inside of it. One is the Amazon Gamelift plugin for unreal, and the other is the Gamelift Cplusplus server SDK.

+ 이제 이 파일들의 압축도 풀어야 하는데요. 문서에도 나와 있듯이, SDK를 기본 다운로드 폴더에 그대로 두고 압축을 풀면 경로 길이가 너무 길어져서(문자 제한 초과) 빌드 오류가 발생할 수 있습니다.
- Now we need to extract these. And as the documentation page says here, putting the SDK in the default download directory can result in build failure due to the path exceeding the character limit.

+ 그래서 문서는 C 드라이브 같은 다른 디렉터리에 SDK를 두는 것을 권장합니다. 우리도 그렇게 하겠습니다. C 드라이브 폴더를 열어주세요.
- They recommend putting the SDK in another directory such as C, and that's what we're going to do. So let's open our C folder.

+ 보시다시피 저는 이미 여러 버전의 GameLift SDK가 있는데요. 헷갈리지 않게 이것들을 C 드라이브에서 옮겨 따로 보관해두고, 새 GameLift Server SDK를 C 드라이브에 압축 풀겠습니다.
- And you can see here that I have several versions of the Gamelift SDK already. Now to avoid confusion I'm going to take these and remove them from my C folder. I'm going to place them elsewhere on my computer for safekeeping, and then I'll extract the new Gamelift server SDK to my C drive.

+ 자, C 드라이브 정리가 끝났으니, 'Amazon GameLift Plugin Unreal Release' 폴더 안의 파일들 압축을 풀겠습니다. 두 개의 폴더가 있는데, 먼저 GameLift C++ Server SDK(버전 5.1.3)입니다.
- Okay, so now that I cleaned up my c drive, I'm going to extract the folders here inside Amazon Gamelift plugin unreal release. We have two folders. We have the Gamelift CP server SDK and this is the SDK 513.

+ C 드라이브에 'GameLift_SDK_513'이라는 이름으로 새 폴더를 하나 만들겠습니다.
- So I'm going to make a folder for that here on C called Gamelift SDK underscore 513.

+ 이 SDK 압축 파일의 내용을 방금 만든 폴더에 풀겠습니다. 우클릭 후 'Extract All'을 누르고, 경로를 복사해 붙여넣은 뒤 'Extract'합니다. 자, GameLift SDK가 준비되었습니다.
- So I'm going to extract the contents of this folder into my Gamelift SDK 513 folder. So I'm going to right click Extract All and I'll just copy this file path, browse paste it in right there and select that folder and extract. And there's the Gamelift SDK.

+ GameLift 플러그인도 똑같이 하겠습니다. 안에 이미 폴더가 생성되어 있으니, C 드라이브 최상위에 바로 풀면 'Amazon GameLift Plugin Unreal' 폴더가 자동으로 생길 겁니다.
- I'm going to do the same thing for the Gamelift plugin. I can see that it has its own Gamelift plugin unreal folder inside of it, so I should be able to just extract it straight to the C drive and it'll create this Amazon Gamelift plugin unreal folder.

+ 우클릭하고 'Extract All', C 드라이브를 선택한 뒤 압축을 풉니다. 이렇게 해서 GameLift SDK와 플러그인 다운로드 및 압축 해제가 끝났습니다.
- So I'm going to right click Extract All and browse. We'll go straight to C, select that folder and extract. And we have now downloaded and extracted the Gamelift SDK and the Amazon Gamelift plugin for unreal.

+ 이제 두 폴더가 준비되었으니 이 단계는 완료되었습니다. OpenSSL과 라이브러리들이 준비되었으니, 다음 단계인 '언리얼 엔진용 C++ 서버 SDK 라이브러리 빌드'를 진행할 차례입니다. 다음 강의에서 뵙겠습니다.
- Now that we have these two folders, we're done with that step. We've downloaded OpenSSL and with these libraries downloaded we're ready for the next step, which is to build the C plus Plus server SDK libraries for Unreal Engine. And we'll do that next. I'll see you soon.
```