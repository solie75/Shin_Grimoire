```diff
- So we now have a API test HUD blueprint.
+ 그래서 우리는 이제 APITestHUD 블루프린트를 가지고 있습니다.

- And it's showing an API test overlay which simply has text.
+ 그리고 그것은 단순히 텍스트만 있는 APITestOverlay를 보여주고 있습니다.

- But we need a widget to show a button we can use to launch our HTTP request and show the results.
+ 하지만 우리는 HTTP 요청을 실행하고 결과를 보여주는 데 사용할 버튼을 보여줄 위젯이 필요합니다.

- So we need a couple of widget classes for that.
+ 그래서 우리는 그것을 위한 위젯 클래스 몇 개가 필요합니다.

- And I'm going to close out of the editor saving all my progress.
+ 모든 진행 상황을 저장하고 에디터를 닫겠습니다.

- So we're going to create a couple more Cplusplus classes.
+ 그래서 우리는 C++ 클래스 몇 개를 더 만들 것입니다.

- We're going to create Cplusplus classes for our widgets so that we have cplusplus level control over them, as we're going to want that sort of control once we start interacting between our classes and sending API calls.
+ 우리가 클래스 간에 상호작용하고 API 호출을 보내기 시작하면 그런 종류의 제어가 필요할 것이므로, 위젯에 대한 C++ 수준의 제어 권한을 갖기 위해 위젯용 C++ 클래스를 만들 것입니다.

- So under our UI folder we're going to have some more classes.
+ 그래서 UI 폴더 아래에 클래스를 몇 개 더 만들 것입니다.

- And the first API call we're going to test is list fleets.
+ 우리가 테스트할 첫 번째 API 호출은 'list fleets'입니다.

- We have an API gateway endpoint called list fleets with a Get method.
+ 우리는 GET 메서드를 가진 'list fleets'라는 API 게이트웨이 엔드포인트를 가지고 있습니다.

- And we also have a lambda connected to that called list fleets.
+ 그리고 우리는 'list fleets'라고 불리는 람다도 거기에 연결해 두었습니다.

- I'd like to create a widget specifically for sending the HTTP request for list fleets, and for showing the results that we get from that.
+ 저는 특히 'list fleets'에 대한 HTTP 요청을 보내고, 거기서 얻은 결과를 보여주기 위한 위젯을 만들고 싶습니다.

- And it is technically a test class as we're just creating it for testing purposes.
+ 그리고 우리가 테스트 목적으로만 만드는 것이기 때문에 기술적으로는 테스트 클래스입니다.

- But it's also more versatile than just a test class.
+ 하지만 단지 테스트 클래스인 것보다는 더 다재다능합니다.

- It's in general just a list fleets related class.
+ 일반적으로 그냥 'list fleets' 관련 클래스입니다.

- Right?
+ 맞죠?

- So I'm going to create a folder called API rather than API test to keep my list fleets related classes.
+ 그래서 저는 제 'list fleets' 관련 클래스들을 보관하기 위해 'APITest'보다는 'API'라는 폴더를 만들 것입니다.

- So I'm going to add a new unreal class.
+ 그래서 새 언리얼 클래스를 추가하겠습니다.

- This will be based on user widget.
+ 이것은 UserWidget을 기반으로 할 것입니다.

- And I'm going to call this class list Fleets box, because I like it to have a scroll box that can populate with more than one widget in case we get multiple fleets back.
+ 그리고 저는 이 클래스를 'ListFleetsBox'라고 부르겠습니다. 왜냐하면 여러 개의 플릿(Fleet)을 돌려받을 경우를 대비해 하나 이상의 위젯으로 채울 수 있는 스크롤 박스를 갖고 싶기 때문입니다.

- And I'd also like this to be in UI slash API, and we'll keep these specific to the API call.
+ 그리고 저는 이것이 'UI/API'에 있기를 원하며, 우리는 이것들을 API 호출에 특화된 것으로 유지할 것입니다.

- I'm going to call this folder list fleets.
+ 저는 이 폴더를 'ListFleets'라고 부르겠습니다.

- So I'm creating an API folder with a list fleets folder inside of it, and we'll have our list fleets box inside of that.
+ 그래서 저는 API 폴더를 만들고 그 안에 ListFleets 폴더를 만들 것이며, 그 안에 ListFleetsBox를 두게 될 것입니다.

- It's kind of a lot of nested folders, but trust me, this makes things a lot easier than having everything jumbled into one folder.
+ 폴더가 많이 중첩되는 것 같지만, 절 믿으세요. 이렇게 하는 게 모든 것을 한 폴더에 뒤섞어 놓는 것보다 훨씬 쉽습니다.

- So we'll create the list fleets box a user widget.
+ 그래서 우리는 UserWidget인 ListFleetsBox를 생성할 것입니다.

- So that's going to go into API under List Fleets and List fleets box is going to have a button and a scroll box.
+ 그래서 그것은 ListFleets 아래의 API 폴더로 들어갈 것이고, ListFleetsBox는 버튼과 스크롤 박스를 갖게 될 것입니다.

- And I'd like those to be variables in C plus plus.
+ 그리고 저는 그것들이 C++ 변수가 되기를 원합니다.

- So we can bind to their callbacks in C plus plus.
+ 그래야 C++에서 콜백에 바인딩할 수 있으니까요.

- But I'd like them to also be connected to blueprints.
+ 하지만 그것들이 블루프린트와도 연결되기를 원합니다.

- We're going to bind these widgets so that we can create a scroll box and a button in blueprint.
+ 우리는 블루프린트에서 스크롤 박스와 버튼을 만들 수 있도록 이 위젯들을 바인딩할 것입니다.

- And then we can have them linked to the C plus plus variable.
+ 그리고 나서 그것들을 C++ 변수에 연결되게 할 수 있습니다.

- So I'm going to just add a public section here.
+ 그래서 저는 여기에 그냥 public 섹션을 추가하겠습니다.

- And I want a scroll box.
+ 그리고 저는 스크롤 박스를 원합니다.

- And I want a t object pointer of this type.
+ 그리고 이 타입의 TObjectPtr를 원합니다.

- So you scroll box and I'm going to call this list Fleets Scroll box.
+ UScrollBox이고 저는 이것을 ListFleetsScrollBox라고 부르겠습니다.

- Optionally you could say scroll box at the beginning.
+ 선택적으로 처음에 ScrollBox라고 붙일 수도 있습니다.

- So scroll box underscore list fleets.
+ ScrollBox_ListFleets 처럼요.

- So either of those conventions is fine.
+ 둘 중 어떤 규칙이든 괜찮습니다.

- However I recommend that when you start making these widgets, you pick one convention and stick to it for the duration of the project.
+ 하지만 일단 위젯을 만들기 시작하면, 하나의 규칙을 정해서 프로젝트 기간 내내 고수하는 것을 권장합니다.

- It's just going to feel better and look nicer.
+ 그게 더 느낌이 좋고 보기에도 좋을 것입니다.

- We'll go ahead and go with the convention of what the widget is, followed by a more specific description for the name.
+ 우리는 위젯이 무엇인지 먼저 쓰고, 그 뒤에 더 구체적인 설명을 붙이는 규칙으로 가겠습니다.

- So scroll box list fleets, and I generally do this only for the widgets that I use bind widget for.
+ 그래서 ScrollBox_ListFleets이고, 저는 보통 BindWidget을 사용하는 위젯들에 대해서만 이렇게 합니다.

- So to bind this cplusplus variable to a widget on the blueprint will make.
+ 그래서 이 C++ 변수를 우리가 만들 블루프린트상의 위젯에 바인딩하기 위해서.

- Out of this, we use Uproperty followed by meta equals and we give this some metadata bind widget.
+ 이것에 UPROPERTY를 사용하고 뒤에 meta=를 붙여서 BindWidget이라는 메타데이터를 줍니다.

- So now we'll be expected to create a scroll box.
+ 이제 우리는 스크롤 박스를 생성해야 할 것입니다.

- List Fleets.
+ ListFleets.

- Scroll box in this list.
+ 이 목록에 있는 스크롤 박스요.

- Fleets box.
+ FleetsBox.

- Blueprint once we make one.
+ 블루프린트를 만들고 나면요.

- Now, I'd like this to be an incomplete type here so we can forward declare it.
+ 자, 여기서 이것을 불완전한 타입(Incomplete Type)으로 만들어서 전방 선언(Forward Declare)을 할 수 있게 하고 싶습니다.

- I'm going to forward declare it up at the top.
+ 맨 위에서 전방 선언을 하겠습니다.

- So we'll just have a simple scroll box.
+ 그래서 우리는 그냥 간단한 스크롤 박스를 갖게 될 것입니다.

- We'll be able to add items to it dynamically based on our HTTP response.
+ 우리는 HTTP 응답에 따라 거기에 항목들을 동적으로 추가할 수 있게 될 것입니다.

- But in addition to a scroll box, I would also like a button.
+ 하지만 스크롤 박스 외에도 버튼 하나가 있었으면 합니다.

- So I'm going to make another t object pointer this time of type U button.
+ 그래서 이번엔 UButton 타입의 TObjectPtr를 하나 더 만들겠습니다.

- And I'm going to call this button underscore List fleets.
+ 그리고 이것을 Button_ListFleets라고 부르겠습니다.

- So this will be the button that we use to list fleets.
+ 이것이 우리가 플릿 목록을 조회하는 데 사용할 버튼이 될 것입니다.

- And it also gets a meta bind widget.
+ 그리고 이것도 BindWidget 메타데이터를 갖습니다.

- So we'll create widgets for these in the list Fleets Box blueprint once we make it.
+ 그래서 ListFleetsBox 블루프린트를 만들면 그 안에 이것들을 위한 위젯을 생성할 것입니다.

- And I'm going to forward declare you button as well.
+ 그리고 UButton도 전방 선언하겠습니다.

- So so far that's all we're going to do for this class.
+ 지금까지는 이 클래스를 위해 할 전부입니다.

- But what are we going to add to the scroll box as children.
+ 그런데 스크롤 박스에 자식으로 무엇을 추가할까요?

- Well it's going to be a basic widget with text, but it would be nice to easily format that text in a widget blueprint and give it a background and any other styling we like.
+ 음, 텍스트가 있는 기본 위젯이겠지만, 위젯 블루프린트에서 그 텍스트의 서식을 쉽게 지정하고 배경이나 원하는 스타일링을 줄 수 있다면 좋을 것입니다.

- So I'd like another widget class for the list fleets text.
+ 그래서 저는 ListFleets 텍스트를 위한 또 다른 위젯 클래스를 원합니다.

- And we know that list fleets returns fleet IDs.
+ 그리고 우리는 'list fleets'가 플릿 ID들을 반환한다는 것을 알고 있습니다.

- So really this is just going to be a basic user widget that we create a blueprint for.
+ 그러니까 이건 사실 우리가 블루프린트를 만들 기본 UserWidget일 뿐입니다.

- And it can be for showing a fleet ID.
+ 그리고 이건 플릿 ID를 보여주기 위한 것일 수 있습니다.

- So under List Fleets in that folder I'm going to make a new unreal class selecting user widget.
+ 그래서 ListFleets 폴더 아래에 UserWidget을 선택해서 새 언리얼 클래스를 만들겠습니다.

- And I'm going to call this widget fleet ID.
+ 그리고 이 위젯을 FleetID라고 부르겠습니다.

- So it'll be U fleet ID under List Fleets.
+ 그래서 ListFleets 아래의 UFleetID가 될 것입니다.

- Here I'm going to go ahead and create that.
+ 여기서 그걸 생성하겠습니다.

- And fleet ID is going to be simple.
+ 그리고 FleetID는 간단할 것입니다.

- We'll have a public section with a T object pointer.
+ TObjectPtr가 있는 public 섹션을 가질 것입니다.

- And this will get a simple text block.
+ 그리고 이건 간단한 텍스트 블록을 갖게 될 것입니다.

- So we're going to use u text block.
+ 그래서 우리는 UTextBlock을 사용할 것입니다.

- And I'm going to call this text block underscore fleet ID.
+ 그리고 이것을 TextBlock_FleetID라고 부르겠습니다.

- And we'll give it the same U property meta bind widget.
+ 그리고 똑같이 BindWidget 메타데이터가 있는 UPROPERTY를 줄 것입니다.

- And we'll go ahead and forward.
+ 그리고 진행하겠습니다.

- Declare the class.
+ 클래스를 선언합니다.

- Class.
+ Class.

- You textblock at the top and that's it.
+ 상단에 UTextBlock을 넣고 그게 다입니다.

- It's going to be simple, but having a C plus plus class makes it easier to create these dynamically from C plus plus and set the text from C plus plus.
+ 간단하겠지만, C++ 클래스를 갖는 것이 C++에서 이것들을 동적으로 생성하고 텍스트를 설정하는 것을 더 쉽게 만들어 줍니다.

- We just need a couple of blueprints.
+ 블루프린트 몇 개만 있으면 됩니다.

- We need the blueprint for the list fleets box widget, and for the fleet ID.
+ ListFleetsBox 위젯과 FleetID를 위한 블루프린트가 필요합니다.

- Let's go ahead and compile and launch the editor and create those widget blueprints for these classes.
+ 컴파일하고 에디터를 실행해서 이 클래스들을 위한 위젯 블루프린트들을 만들어 봅시다.

- Okay.
+ 좋아요.

- So in the editor we're going to go back to our dedicated servers folder.
+ 에디터에서 Dedicated Servers 폴더로 돌아갈 것입니다.

- And we have our API test right next to it.
+ 그리고 바로 옆에 APITest가 있습니다.

- I'm going to make an API folder and an API folder.
+ 저는 API 폴더를 만들 것입니다.

- I'll make a new folder called List Fleets and we'll have List fleets related classes here.
+ ListFleets라는 새 폴더를 만들고 여기에 ListFleets 관련 클래스들을 두겠습니다.

- So I'm first going to make a new widget blueprint based on List Fleets box.
+ 먼저 ListFleetsBox를 기반으로 새 위젯 블루프린트를 만들겠습니다.

- And I'm going to call this WP List Fleets box.
+ 그리고 이것을 WP_ListFleetsBox라고 부르겠습니다.

- And I'm going to make another widget blueprint based on fleet ID called BP fleet ID.
+ 그리고 FleetID를 기반으로 BP_FleetID라는 또 다른 위젯 블루프린트를 만들겠습니다.

- Now these require some widgets inside them thanks to meta bind widget.
+ 이제 이것들은 Meta BindWidget 덕분에 내부에 몇 가지 위젯을 필요로 합니다.

- We'll start with list fleets box.
+ ListFleetsBox부터 시작하겠습니다.

- It needs a scroll box.
+ 스크롤 박스가 필요합니다.

- I'm going to go ahead and add a scroll box.
+ 스크롤 박스를 추가하겠습니다.

- But we also need a button.
+ 하지만 버튼도 필요합니다.

- And those aren't the only widgets that we can put in here.
+ 그리고 우리가 여기에 넣을 수 있는 위젯이 그것들만 있는 건 아닙니다.

- So I'd like to format this a little bit so I have a bit more control.
+ 그래서 저는 제어를 좀 더 잘하기 위해 서식을 조금 지정하고 싶습니다.

- I'm going to start with a size box.
+ 사이즈 박스(Size Box)로 시작하겠습니다.

- I'm going to get this size box at the dimensions I want.
+ 이 사이즈 박스를 제가 원하는 치수로 만들겠습니다.

- I'm going to go up to fill screen and change to desired.
+ 'Fill Screen(화면 채우기)'으로 가서 'Desired(원하는 크기)'로 바꾸겠습니다.

- That's going to shrink it down.
+ 그러면 크기가 줄어들 것입니다.

- But on the size box details panel I'm going to check width and height override and drag these out so that they're a little larger.
+ 하지만 사이즈 박스 디테일 패널에서 Width Override와 Height Override를 체크하고 이것들을 드래그해서 좀 더 크게 만들겠습니다.

- And in the size box I'd like both my scroll box and my button.
+ 그리고 사이즈 박스 안에 스크롤 박스와 버튼 둘 다 넣고 싶습니다.

- And if I want to stack things vertically, a vertical box is fine for that.
+ 그리고 이것들을 수직으로 쌓고 싶다면, 버티컬 박스(Vertical Box)가 적당합니다.

- So I'm going to bring in a vertical box, and in my vertical box I can put my scroll box.
+ 그래서 버티컬 박스를 가져오고, 그 안에 스크롤 박스를 넣을 수 있습니다.

- So I'm going to put the scroll box next.
+ 그래서 다음에 스크롤 박스를 넣겠습니다.

- And this has to be renamed just to remind ourselves in list fleets box.
+ 그리고 이것은 ListFleetsBox 안에서 우리 스스로를 상기시키기 위해 이름을 바꿔야 합니다.

- This is the exact name it has to have thanks to Meta Bind widget.
+ Meta BindWidget 덕분에 정확히 이 이름을 가져야 합니다.

- So it's going to be scroll box underscore list fleets.
+ 그래서 ScrollBox_ListFleets가 될 것입니다.

- I'm going to rename it first thing.
+ 제일 먼저 이름을 바꾸겠습니다.

- And just under the scroll box I need the button.
+ 그리고 스크롤 박스 바로 아래에 버튼이 필요합니다.

- So searching for button I'm going to bring that onto the vertical box.
+ 버튼을 검색해서 버티컬 박스 위로 가져오겠습니다.

- So it's right there beneath it.
+ 그래서 바로 그 아래에 있습니다.

- And this one has to be called button List fleets.
+ 그리고 이건 Button_ListFleets라고 불러야 합니다.

- Look at this writer is even complaining about this.
+ 보세요, Rider조차도 이에 대해 불평하고 있네요.

- There's a required widget binding that I didn't make right away.
+ 제가 바로 만들지 않은 필수 위젯 바인딩이 있다는군요.

- We get those errors until we do this.
+ 우리가 이걸 할 때까지 그런 에러들이 뜹니다.

- So I'm going to rename this to button List fleets.
+ 그래서 이것을 Button_ListFleets로 이름을 바꾸겠습니다.

- And now we've satisfied those requirements.
+ 이제 우리는 그 요구사항들을 충족시켰습니다.

- Now we can format this any way we like.
+ 이제 우리는 이것을 원하는 대로 서식을 지정할 수 있습니다.

- This is just for testing purposes.
+ 이건 단지 테스트 목적일 뿐입니다.

- I'm not going to put a whole lot of effort in, but I'm going to set them both to fill and set my scroll box to just take up a little bit more space.
+ 공을 많이 들이지는 않겠지만, 둘 다 'Fill(채우기)'로 설정하고 스크롤 박스가 공간을 조금 더 차지하도록 설정하겠습니다.

- I'll treat these as percentages for the size, and give the scroll box 0.75, and give the box 0.25 of the space.
+ 크기를 비율로 처리해서, 스크롤 박스에 0.75, 박스(버튼)에 0.25의 공간을 주겠습니다.

- I'm also going to check center align horizontally and center align vertically, and I'll give it some padding on the top and the bottom.
+ 또한 수평 중앙 정렬과 수직 중앙 정렬을 체크하고, 위아래에 패딩을 좀 주겠습니다.

- Let's just say five and five.
+ 그냥 5와 5라고 하죠.

- We'll experiment with this.
+ 이걸로 실험해 보겠습니다.

- But this is more than fine.
+ 하지만 이 정도면 충분합니다.

- I really am not going to see how big this truly looks until I put one of these into my overlay, so we'll probably make more adjustments later.
+ 오버레이에 넣어보기 전까지는 진짜 크기가 어떻게 보이는지 알 수 없으니, 아마 나중에 더 조정하게 될 것입니다.

- Now my button can have some text in it, so I'm going to go ahead and add the text onto the button.
+ 이제 버튼에 텍스트를 넣을 수 있으니, 버튼 위에 텍스트를 추가하겠습니다.

- And I want it to be a child of the button.
+ 그리고 그게 버튼의 자식이 되기를 원합니다.

- So it's in the button itself.
+ 그래서 버튼 자체 안에 있는 것이죠.

- And I'm realizing that this widget is probably pretty small, I'm going to resize it a bit more.
+ 그리고 이 위젯이 꽤 작을 것 같다는 생각이 들어서 크기를 좀 더 조정하겠습니다.

- Yeah, 100 pixels is too small.
+ 네, 100픽셀은 너무 작습니다.

- So I'm going to make it bigger and I'm going to change the text to say list fleets and I'll make it a little bit smaller.
+ 그래서 더 크게 만들고 텍스트를 'List Fleets'라고 바꾸고 글자를 조금 더 작게 만들겠습니다.

- Change the styling a bit.
+ 스타일을 좀 바꾸겠습니다.

- Give it an outline so it's easier to see and so on.
+ 보기 편하게 외곽선을 주는 등등이요.

- So we have a button and a scroll box.
+ 그래서 우리는 버튼과 스크롤 박스를 가지고 있습니다.

- The scroll box is basically invisible.
+ 스크롤 박스는 기본적으로 보이지 않습니다.

- We can of course wrap all this in an overlay if we'd like to put an image behind it.
+ 뒤에 이미지를 넣고 싶다면 당연히 이 모든 것을 오버레이로 감쌀 수 있습니다.

- So why not.
+ 안 될 거 없죠.

- I'm going to take my vertical box and wrap that with an overlay.
+ 버티컬 박스를 잡고 오버레이로 감싸겠습니다.

- Make my vertical box horizontal and vertical alignment both set to fill up the space.
+ 버티컬 박스의 수평, 수직 정렬을 모두 공간을 채우도록(Fill) 설정하겠습니다.

- And then my overlay can now take an image.
+ 그러면 이제 오버레이가 이미지를 가질 수 있습니다.

- I'll put it just above the vertical box, set it to fill, and basically just change its color and opacity to something that's going to make it a little bit easier to see.
+ 버티컬 박스 바로 위에 놓고, 채우기(Fill)로 설정한 다음, 기본적으로 색상과 투명도를 좀 더 보기 쉬운 색으로 바꾸겠습니다.

- And I'll just get my scroll box and give it a little padding itself.
+ 그리고 스크롤 박스를 잡고 자체적으로 패딩을 좀 주겠습니다.

- So there we go.
+ 자, 됐습니다.

- Like I said, not a whole lot of effort, but you know, it's something we can see.
+ 말했듯이, 공을 많이 들이진 않았지만 뭐, 눈에 보이는 무언가는 됐습니다.

- So I have this list fleets box.
+ 그래서 이 ListFleetsBox가 생겼습니다.

- I now need to make my WP fleet ID compliant with the requirement imposed by bind widget.
+ 이제 BindWidget에 의해 부과된 요구사항을 준수하도록 WP_FleetID를 만들어야 합니다.

- Namely, I need a text block called text box fleet ID.
+ 즉, TextBlock_FleetID라는 이름의 텍스트 블록이 필요합니다.

- And for this I'm just quickly going to add a size box.
+ 그리고 이걸 위해 저는 빨리 사이즈 박스를 추가하겠습니다.

- You have freedom here.
+ 여기선 자유롭게 하셔도 됩니다.

- You can make these styled any way you want.
+ 원하는 대로 스타일을 만드실 수 있습니다.

- The details of how we style these is not important in the least, and really depends on your game, but I'm going to add the size box while we're at it.
+ 스타일링의 세부 사항은 전혀 중요하지 않고 정말 여러분의 게임에 따라 다르지만, 저는 하는 김에 사이즈 박스를 추가하겠습니다.

- I'll add an overlay.
+ 오버레이를 추가하겠습니다.

- Just so that I can have an image for my background.
+ 배경 이미지를 가질 수 있도록요.

- The image will be filling the space.
+ 이미지가 공간을 채울 것입니다.

- I'm going to give it a color that's slightly different and we need the text block.
+ 약간 다른 색상을 주고 텍스트 블록이 필요합니다.

- It's just called text in the blueprint editor.
+ 블루프린트 에디터에서는 그냥 'Text'라고 되어 있습니다.

- I'm going to put it on the overlay and immediately rename it to Text block Fleet ID like so, and just give it some default text.
+ 이걸 오버레이 위에 올리고 즉시 TextBlock_FleetID처럼 이름을 바꾼 다음 기본 텍스트를 좀 넣겠습니다.

- I'm just going to say fleet ID that's all I'm going to say.
+ 그냥 'Fleet ID'라고만 하겠습니다.

- I'm going to center align vertically and change it from bold to regular.
+ 수직으로 가운데 정렬하고 굵은 글씨를 일반으로 바꾸겠습니다.

- Give it an outline size.
+ 외곽선 크기를 줍니다.

- And we have our basic fleet ID widget.
+ 그리고 우리 기본 FleetID 위젯이 생겼습니다.

- I'm going to give it some left padding of five, and we can see what this looks like in our scroll box.
+ 왼쪽 패딩을 5 정도 주고, 이게 스크롤 박스에서 어떻게 보이는지 확인할 수 있습니다.

- By adding a couple, all we have to do is search for ABP fleet ID and just drag one in and we can see what they look like.
+ 몇 개를 추가해 보면 되는데, BP_FleetID를 검색해서 그냥 드래그해 넣으면 어떻게 생겼는지 볼 수 있습니다.

- And I can see that it might be a little too large compared to our list fleets widget, so I can simply bring that font size down and change my size box height override.
+ ListFleets 위젯에 비해 좀 너무 큰 것 같아서, 폰트 크기를 줄이고 사이즈 박스 높이 오버라이드를 변경하면 됩니다.

- Compile that and we can see that reflected here.
+ 컴파일하면 여기에 반영된 것을 볼 수 있습니다.

- So we don't need a fleet ID here.
+ 그래서 여기에는 FleetID가 필요 없습니다.

- We're going to end up adding these dynamically once we get the HTTP response.
+ 우리는 HTTP 응답을 받으면 이것들을 동적으로 추가하게 될 것입니다.

- But what we can do right now is add a Wbhp list fleets box to our test overlay an API test in UI.
+ 하지만 지금 할 수 있는 건 UI의 APITest에 있는 테스트 오버레이에 WP_ListFleetsBox를 추가하는 것입니다.

- I'm going to go to Wbhp API test overlay and search for Wbhp list fleets box and just drag one in.
+ WP_APITestOverlay로 가서 WP_ListFleetsBox를 검색해서 하나 드래그해 넣겠습니다.

- It's a little small, but that's fine.
+ 좀 작지만 괜찮습니다.

- This is for testing purposes.
+ 이건 테스트 목적이니까요.

- We'll just grab one and put it there.
+ 그냥 하나 잡아서 거기에 두겠습니다.

- We'll anchor it to the center.
+ 중앙에 앵커를 걸겠습니다.

- Why not?
+ 안 될 거 없죠?

- And we can see how it looks when we press play.
+ 그리고 플레이를 누르면 어떻게 보이는지 볼 수 있습니다.

- And there it is.
+ 저기 있네요.

- I could have made it a bit bigger, but that's okay.
+ 좀 더 크게 만들 수도 있었지만, 괜찮습니다.

- It's just for testing purposes.
+ 이건 그냥 테스트 목적이니까요.

- So next we need to give this list fleets box some functionality.
+ 그래서 다음에는 이 ListFleetsBox에 기능을 좀 부여해야 합니다.

- We want to make an API call.
+ 우리는 API 호출을 하고 싶습니다.

- So we're getting really close to that new stuff that you don't know how to do yet.
+ 이제 여러분이 아직 할 줄 모르는 그 새로운 기능에 정말 가까워지고 있습니다.
```